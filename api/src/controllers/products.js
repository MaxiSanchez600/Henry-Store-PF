const { Product } = require('../db');

const productController = {
    getAll: (req, res, next) => {
        try {
            
        } catch (error) {
            return next(error);
        }
    }
};

module.exports = productController;